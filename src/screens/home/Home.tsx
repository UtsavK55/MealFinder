import {Text} from 'react-native';

import BaseContainer from '@components/baseContainer';

const Home = () => {
  return (
    <BaseContainer>
      <Text>Home</Text>
    </BaseContainer>
  );
};

export default Home;
